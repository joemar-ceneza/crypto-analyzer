"""
Dashboard views — one module per view, each exposing a single render() entry.

The router in app.py decides which one runs; views never import each other,
except that the Scorecard view owns its Breakdown and Calibration sections.
"""
